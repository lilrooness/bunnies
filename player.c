#include "player.h"

void update(Player *player, char *map) {
    // player->xpos += player->xdir;
    // if(check_collisions(player)) {
    //     player->xpos -= player->xdir;
    // }

    // player->ypos += player->ydir;
    // if(check_collisions(player)) {
    //     player->ypos -= player->ydir;
    // }
}


bool check_collisions(Player *player, char *map, int width, int stride) {
    //TODO collide with map
    return false;
}